spring:
  cloud:
    openfeign:
      client:
        config:
          book:
            url: http://localhost:8081
  datasource:
    url: jdbc:h2:mem:rental;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
